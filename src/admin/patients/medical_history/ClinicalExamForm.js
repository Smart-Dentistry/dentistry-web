import React from 'react'
import PropTypes from 'prop-types'

const ClinicalExamForm = (prev) => <h1>Clinical Exam</h1>

ClinicalExamForm.propTypes = {
  prev: PropTypes.func
}

export default ClinicalExamForm
